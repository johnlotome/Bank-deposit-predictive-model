from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a model to predict if a customer will subscribe to term deposit',
    author='lotome John',
    license='MIT',
)
